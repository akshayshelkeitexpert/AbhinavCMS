/**
 * Local / development environment
 * API runs on Docker (e.g. http://localhost:8080/api/v1) or local PHP server
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080/api/v1',
  apiCommtext: 'TCDS_123!!',
};
